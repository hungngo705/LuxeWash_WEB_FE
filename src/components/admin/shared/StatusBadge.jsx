/**
 * Re-export of the LuxeWash StatusBadge primitive so legacy imports
 * (`../../components/admin/shared/StatusBadge`) keep working while the
 * codebase gradually moves to `../../components/ui/StatusBadge`.
 */
export { default } from '../../ui/StatusBadge'